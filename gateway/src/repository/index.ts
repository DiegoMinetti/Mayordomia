/**
 * Public surface of the repository module. Handlers import from here.
 */
export { makeRepository, newId, type Repository } from './repository.js';
export { COLUMNS, columnMapFor, type ColumnMap } from './columns.js';
